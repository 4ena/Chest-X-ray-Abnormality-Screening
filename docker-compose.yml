version: '3.8'
services:
  backend:
    build:
      context: backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      # Mount models dir so you can drop .pth without rebuilding
      - ./models:/app/models
    environment:
      - PYTHONUNBUFFERED=1
